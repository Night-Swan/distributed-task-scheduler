info:
  name: Submit PDF Job
  type: http
  seq: 6

http:
  method: POST
  url: ttp://localhost:8080/jobs/pdf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
