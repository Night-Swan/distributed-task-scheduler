info:
  name: Test Whisper
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/v1/audio/transcriptions
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/nightswan/test.wav
        contentType: audio/wav
      - name: model
        type: text
        value: Systran/faster-whisper-small
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
