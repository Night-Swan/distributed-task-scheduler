info:
  name: Submit Embedding Job
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/jobs
  body:
    type: json
    data: |-
      {
          "job_type": "embedding",
          "prompt": "The theory of relativity",
          "submitted_by": "nightswan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
